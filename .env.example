# Copy to .env.local for local scripts / optional CLI defaults (never commit .env.local).
# The web UI stores the Zoo token in localStorage and sends it per request.

ZOO_API_TOKEN=

# Optional: where job files are stored (default ./data)
# MESHMOOSE_DATA_DIR=./data
